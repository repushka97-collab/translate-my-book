# 🔍 Проблемы проекта и решения

## 🚨 Критические проблемы

### 1. Низкое качество метрик (Layout Preservation: 57.2%)

**Проблема:**
- Layout Preservation: 57.2% (нужно >85%)
- File Size Ratio: 2.39x (нужно <1.5x)
- Quality Score: 72/100 (нужно >90/100)

**Причины:**
- HTML flow layout не сохраняет точные позиции
- Изображения смещаются при переводе
- Текст переполняет границы блоков

**Решения:**
1. ✅ Использовать `HTML_ABSOLUTE=1` вместо flow layout
2. ✅ Улучшить алгоритм сравнения в `layout_similarity.py` (уже сделано)
3. ✅ Применять более агрессивное сжатие Ghostscript
4. ⚠️ Улучшить `pixel_perfect_diff.py` для более точного сравнения

---

### 2. Проблемы с кириллицей в PDF_REBUILD_V2

**Проблема:**
- Стандартные шрифты PyMuPDF не поддерживают кириллицу
- Font Manager не находит системные шрифты

**Причины:**
- Шрифты DejaVu/Arial не найдены в стандартных путях Windows
- Font Manager ищет только в ограниченных директориях

**Решения:**
1. ✅ Улучшить поиск шрифтов в `font_manager.py` (уже сделано)
2. ⚠️ Добавить больше путей поиска для Windows
3. ⚠️ Использовать встроенные шрифты Windows (Arial, Times New Roman)
4. ✅ Рекомендовать HTML→PDF для кириллицы (уже работает)

---

### 3. Большой размер файлов (File Size Ratio: 2.39x)

**Проблема:**
- Переведенные PDF в 2.39 раза больше оригиналов
- Нет эффективного сжатия

**Причины:**
- Ghostscript не применяется достаточно агрессивно
- Изображения не оптимизируются
- Шрифты не сжимаются

**Решения:**
1. ✅ Добавить более агрессивное сжатие в `exporter.py` (уже сделано)
2. ⚠️ Оптимизировать изображения перед вставкой
3. ⚠️ Использовать `deflate_images=True` и `deflate_fonts=True`
4. ⚠️ Применять Ghostscript с настройками `/screen` или `/ebook`

---

## ⚠️ Средние проблемы

### 4. Отсутствие обработки ошибок

**Проблема:**
- Много `except: pass` без логирования
- Ошибки теряются

**Решения:**
1. ⚠️ Добавить логирование всех исключений
2. ⚠️ Использовать `ERROR_LOG_PATH` для всех ошибок
3. ⚠️ Добавить retry механизмы для критичных операций

---

### 5. Проблемы с кодировкой в Windows

**Проблема:**
- Unicode символы не отображаются в PowerShell
- Ошибки при выводе в консоль

**Решения:**
1. ✅ Добавить `sys.stdout.reconfigure(encoding='utf-8')` (уже сделано в некоторых местах)
2. ⚠️ Добавить во все скрипты
3. ⚠️ Использовать ASCII символы вместо Unicode в print

---

### 6. Зависимости не установлены

**Проблема:**
- pdf-diff не установлен (требует C++ Build Tools)
- mathpix не установлен (есть fallback)

**Решения:**
1. ✅ Все имеют fallback (уже реализовано)
2. ⚠️ Добавить проверку зависимостей при старте
3. ⚠️ Показывать предупреждения о недостающих библиотеках

---

## 💡 Улучшения

### 7. Производительность

**Проблема:**
- Медленная обработка больших PDF
- Нет кэширования промежуточных результатов

**Решения:**
1. ✅ Использовать `parallel_pdf_processor.py` (уже есть)
2. ⚠️ Добавить кэширование переводов
3. ⚠️ Оптимизировать обработку изображений

---

### 8. Качество переводов

**Проблема:**
- Нет проверки качества переводов
- Нет валидации результатов

**Решения:**
1. ✅ Использовать `quality_scorecard.py` (уже есть)
2. ⚠️ Добавить автоматическую коррекцию при низком качестве
3. ⚠️ Добавить предупреждения о проблемных страницах

---

## 📋 План действий

### Приоритет 1 (Критично):
1. ✅ Улучшить поиск шрифтов для кириллицы
2. ⚠️ Оптимизировать сжатие PDF (Ghostscript)
3. ⚠️ Исправить алгоритм сравнения layout

### Приоритет 2 (Важно):
4. ⚠️ Добавить логирование ошибок
5. ⚠️ Исправить проблемы с кодировкой
6. ⚠️ Добавить проверку зависимостей

### Приоритет 3 (Улучшения):
7. ⚠️ Оптимизировать производительность
8. ⚠️ Улучшить качество переводов

