# Core ML + text
torch
transformers
sentencepiece
safetensors
regex
tqdm

# PDF ingest
pymupdf          # fitz
pdfplumber
pypdf
pdfminer.six
pdf2docx
ocrmypdf

# Export
python-docx
PyYAML
Pillow  # Для обработки изображений

# Vector graphics and image processing
opencv-python-headless
svgwrite
svgpathtools
scikit-image
weasyprint

# ML/AI для документов
layoutparser[all]
doctr
torchvision

# Специализированные инструменты
# mathpix  # Недоступен через pip, требует отдельной установки
tabula-py
camelot-py[cv]
fonttools
colour-science

# Оптимизация и профилирование
dask
joblib
memory-profiler
pdf-diff
# perceptualdiff  # Недоступен через pip для Windows (используем opencv-python + scikit-image)
# structural-similarity  # Недоступен через pip (используем scikit-image.metrics.structural_similarity)
